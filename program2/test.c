#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main()
{
  /*
  srand(time(NULL));
  int randRooms[10];
  for(int j = 0; j < 10; j++)
  {
    randRooms[j] = j;
  }
  for (int i = 10 - 1; i > 0; --i)
  {
    // generate random index
    int w = rand() % i;
    // swap items
    int t = randRooms[i];
    randRooms[i] = randRooms[w];
    randRooms[w] = t;
  }
  for(int k = 0; k < 7; k++)
  {
    printf("the number is %d\n", randRooms[k]);
  }*/
  int a = 20;
while( a <= 20 )
  {
      printf("value of a: %d\n", a);
      a = a + 1;
   }
   return 0;

}
